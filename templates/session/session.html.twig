{% extends 'base.html.twig' %}

{% block title %}Formations{% endblock %}

{% block body %}
<div class="uk-container">

    {# session #}
    {% if slug == 'session' %}

    <h1>Liste des sessions</h1>

    {% if sessions|default %}

    <table class="uk-table uk-table-responsive uk-table-hover uk-table-divider uk-table-small">
        <thead>
            <tr>
                <th>id</th>
                <th>Formation id:</th>
                <th>Places:</th>
                <th>Date début</th>
                <th>Date fin</th>
                <th>Inscrits</th>
            </tr>
        </thead>
        <tbody>


            {% for session in sessions %}
            <tr>
                <td><a href="{{ path('detail_session', {'id': session.id }) }}">{{ session.id }}</a></td>
                <td><a href="{{ path('detail_session', {'id': session.id }) }}">{{ session.formation.id }}</a></td>
                <td><a href="{{ path('detail_session', {'id': session.id }) }}">{{ session.maxstudents }}</a></td>
                <td><a href="{{ path('detail_session', {'id': session.id }) }}">{{ session.datestart | date("j-m-Y")
                        }}</a></td>
                <td><a href="{{ path('detail_session', {'id': session.id }) }}">{{ session.dateend | date("j-m-Y")
                        }}</a></td>
                <td><a href="{{ path('detail_session', {'id': session.id }) }}">{{ session.reservations }}</a></td>
                </a>
            </tr>

            {% endfor %}

        </tbody>
    </table>

    {% else %}
    <h2>Aucune informations en base de données</h2>
    {% endif %}
    {% endif %}

    {% if slug == 'formation' %}


    {# formation #}
    {% if formations|default %}

    <h1>Liste des formations</h1>
    <div id="app"
        class="uk-grid-small uk-animation-fade uk-flex-middle uk-child-width-1-9 uk-child-width-1-2@m uk-child-width-1-3@l uk-padding-small"
        uk-grid>
        {# <div class="uk-child-width-1-2@s uk-child-width-1-3@m uk-grid-small uk-grid-match" uk-grid> #}
            {% for formation in formations %}
            <div>
                <div class="uk-card uk-card-default uk-card-hover">
                    {# <div class="uk-card-header">
                        <div class="uk-grid-small uk-flex-middle" uk-grid>
                            <div class="uk-width-auto">
                                <img width="60" height="60"
                                    src="https://upload.wikimedia.org/wikipedia/en/thumb/d/d6/BT_logo.svg/1280px-BT_logo.svg.png">
                            </div>
                            <div class="uk-width-expand">
                                <h3 class="uk-card-title uk-margin-remove-bottom">{{ formation.name }}</h3>
                                <p class="uk-text uk-margin-remove"><time datetime="2016-04-01T19:00"></time></p>
                                <p class="uk-text-meta uk-margin-remove-top"><time datetime="2016-04-01T19:00">Inscrits:
                                        0, Durée: 0</time></p>
                            </div>
                        </div>
                    </div> #}
                    <div class="uk-card-body">
                        <div class="uk-grid-small uk-flex-middle" uk-grid>
                            <div class="uk-width-auto">
                                <img width="60" height="60"
                                    src="https://upload.wikimedia.org/wikipedia/en/thumb/d/d6/BT_logo.svg/1280px-BT_logo.svg.png">
                            </div>
                            {{ formation.name }}
                            <!-- Le contenu de la carte va ici -->
                        </div>
                    </div>
                </div>
            </div>

            {# <div>
                <a href="">
                    <div class="uk-card uk-card-default">
                        <div class="uk-card-media-top" style=""></div>
                        <div class="uk-card-body">
                            <h3 class="uk-card-title">{{ formation.name }}</h3>
                            <p>Autre infos session</p>
                        </div>
                    </div>
                </a>
            </div> #}
            {% endfor %}
        </div>

        {% else %}
        <h2>Aucune informations en base de données</h2>
        {% endif %}
        {% endif %}
    </div>
    {% endblock %}