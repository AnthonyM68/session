{% extends 'base.html.twig' %}

{% block title %}Formations{% endblock %}

{% block body %}

{# formation #}
{% if formations|default %}
<div class="uk-container">
    <h1>Liste des formations</h1>

    <div class="uk-child-width-1-2@s uk-child-width-1-3@m uk-grid-small uk-grid-match uk-animation-fade" uk-grid>
        {% for formation in formations %}

        <a href="{{ path('detail_program', {'id': program.id }) }}" <div id="uk-card-formation"
            class="uk-card uk-card-default uk-card-hover uk-animation-slide-bottom-small">
            <div class="uk-card-body">
                <div class="uk-grid-small " uk-grid>

                    <div class="uk-width-auto">
                        <img width="70" height="70"
                            src="https://upload.wikimedia.org/wikipedia/en/thumb/d/d6/BT_logo.svg/1280px-BT_logo.svg.png">
                    </div><br>

                    <div class="uk-width-expand">
                        <h3 class="uk-card-title">{{ formation.name }}</h3>
                        {# <p class="uk-text uk-margin-remove"><time datetime="2016-04-01T19:00"></time></p>
                        <p class="uk-text-meta uk-margin-remove-top"><time datetime="2016-04-01T19:00"></time></p> #}
                    </div>
                </div>
            </div>

        </a>
        {% endfor %}
    </div>
    {% else %}
    <h2>Aucune informations en base de données</h2>
    {% endif %}

</div>





{# list des sessions #}





{% endblock %}